{
  "name": "enthusiast-2",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/sevarubbo/enthusiast-2.git",
  "license": "MIT",
  "scripts": {
    "lint": "eslint . --fix --report-unused-disable-directives",
    "check-types": "tsc --noEmit",
    "verify": "yarn check-types && yarn lint --fix",
    "build": "sh scripts/build.sh",
    "start": "esbuild source/index.ts --bundle --outfile=dist/bundle.js --servedir=dist --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.14.43",
    "eslint": "^8.17.0",
    "eslint-config-seva": "sevarubbo/eslint-config-seva",
    "typescript": "^4.7.3"
  }
}
